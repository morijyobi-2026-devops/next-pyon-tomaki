{
  "name": "next-pyon-tomaki",
  "version": "1.0.0",
  "description": "Documentation and linting setup for next-pyon-tomaki",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "lint": "markdownlint-cli2 \"README.md\" \"docs/**/*.md\" && pnpm --filter next-pyon-tomaki-app lint",
    "build": "pnpm --filter next-pyon-tomaki-app build"
  },
  "lint-staged": {
    "*.md": "markdownlint-cli2"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "markdownlint-cli2": "^0.22.0",
    "prisma": "^7.0.0"
  },
  "dependencies": {
    "@prisma/client": "^7.0.0"
  }
}
